import { StoreCatalog } from "./StoreCatalog";

export const metadata = {
  title: "Store",
  description:
    "Buy MATRIX for Mac — perpetual license, 14-day trial, two seats. Sign in with Google and pay with PayPal.",
};

export default function StorePage() {
  return <StoreCatalog />;
}
