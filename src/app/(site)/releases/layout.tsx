export const metadata = {
  title: "Releases & Downloads",
  description:
    "Download MATRIX for Mac. Installer builds, release notes, and system requirements.",
};

export default function ReleasesLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
