/**
 * MATRIX media slots (legacy folder name /public/hydra/).
 * Chapter stages prefer live WebGL (`MatrixChapterVisual`) over stills.
 * PNGs remain optional offline / OG fallbacks only.
 *
 * hero-studio.png|.mp4     — Optional OG / legacy fallback
 * chapter-matrix.png|.mp4  — Optional fallback (Patch field scene is procedural)
 * chapter-bridges.png|.mp4 — Optional fallback
 * chapter-control.png|.mp4 — Optional fallback
 * /matrix/brand-mark.png   — Brand mark (from app)
 */
